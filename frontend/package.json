{
  "name": "mmm-whatcha-say",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://mjhackathonaccount.github.io/mmmWhatchaSay",
  "dependencies": {
    "@noble/hashes": "^1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tone": "^14.8.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build --dotfiles",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "react-scripts": "5.0.1"
  }
}
